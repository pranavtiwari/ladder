If a ladder player reports a match result, and the opponents have not joined the ladder yet, then don't keep this waiting for opponent approval, and move it to accepted right away. 

fix: insert or update on table "teams" violates foreign key constraint "teams_player2_id_fkey"

Move user profile from left nav, to top right. Keep it consistent on all pages.

On left nav, add a button to 'report an issue'. Take the user to a page with text box where the user can describe the issue. Save the issue in an issues table.

sort - elo rating vs ladder rank: make this a drop down selection instead of toggle

any reported match that is more than 24 hours old automatically accepted

this message is confusing: Ranked matches update ELO. Unscheduled matches shift ±1 rank.

